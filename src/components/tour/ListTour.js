import React from "react";

const ListTour = (props) => {
    return (
        <div>
            <span style={{ fontSize: "50px" }}>Content of List</span>
        </div>
    );
};

export default ListTour;
